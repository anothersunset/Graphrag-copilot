# 产品架构说明

GraphRAG Copilot 包含文档解析服务、向量检索服务、BM25 检索服务、知识图谱服务和问答编排服务。

文档解析服务依赖 OCR 和 ASR 处理多模态数据。

向量检索服务使用 FAISS 存储文本向量。

BM25 检索服务用于关键词和专有名词匹配。

知识图谱服务使用 Neo4j 存储实体关系。

问答编排服务通过 Multi-Agent 管线完成问题理解、检索、推理、验证和生成。
